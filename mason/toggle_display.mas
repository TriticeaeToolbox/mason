<%args>
    $production => undef
    $sandbox => undef
</%args>

% if ( our $T3_INSTANCE_TYPE eq "PRODUCTION" && defined $production  ) {
<& $production &>
% } elsif ( $T3_INSTANCE_TYPE eq "SANDBOX" && defined $sandbox ) {
<& $sandbox &>
% }